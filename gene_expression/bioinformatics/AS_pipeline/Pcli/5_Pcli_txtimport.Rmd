---
title: "Pcli_txtimport"
author: "allyson_demerlis"
date: "2023-07-28"
output: html_document
---

```{r}
library("tximport") #requires BiocManager
library("readr")
library("tximportData")
library("readxl")


dir <- system.file("extdata", package = "tximportData")
list.files(dir)

samples <- read_csv("Pcli_samples.csv")

files <- file.path(dir="~/Documents/GitHub/Ch2_temperaturevariability2023/gene_expression/bioinformatics/AS_pipeline/Pcli/", "salmon", samples$`Fastq file name`, "quant.sf")
names(files) <- paste0("sample", 1:6)
all(file.exists(files))

files

 tximport(files, type = "salmon", tx2gene = tx2gene)

tx2gene <- read_tsv(file = "quant.sf")
```

